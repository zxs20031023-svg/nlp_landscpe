# 使用说明

本文档用于指导用户运行并使用“景观设计需求转译与场地解析工作流”。

## 1. 项目简介

本系统面向景观设计前期分析场景，支持以下两类核心任务：

- 将自然语言景观设计需求转译为结构化数字化任务书
- 将场地描述或勘察文件解析为结构化场地分析报告

系统采用 `Streamlit + LangChain + OpenAI Compatible API + ChromaDB` 实现，支持规范知识库检索、显式规则校验、JSON 导出和历史结果复用。

## 2. 运行环境

建议环境：

- Python 3.10 及以上
- Windows / macOS / Linux
- 可访问兼容 OpenAI 协议的大模型服务

## 3. 安装依赖

在项目根目录执行：

```bash
pip install -r requirements.txt
```

如果你使用虚拟环境，建议先激活虚拟环境再安装依赖。

## 4. 启动项目

在项目根目录执行：

```bash
streamlit run app.py
```

启动后，终端会显示本地访问地址，通常为：

```text
http://localhost:8501
```

在浏览器中打开该地址即可进入系统界面。

## 5. 首次使用前准备

进入页面后，需要先在左侧边栏填写以下参数：

- `API Key`
- `Base URL`
- `主模型`
- `嵌入模型`

说明：

- `API Key` 为你所使用模型服务平台提供的密钥
- `Base URL` 为兼容 OpenAI 协议的接口地址
- `主模型` 用于生成任务书和场地分析
- `嵌入模型` 用于构建规范知识库向量检索

默认 `Base URL` 已预置为阿里云百炼兼容地址，如你使用其他平台，可按实际服务地址修改。

## 6. 功能使用流程

### 6.1 规范知识库管理

该步骤建议优先完成。

操作步骤：

1. 打开“知识库管理”标签页
2. 上传规范文件
3. 点击“构建或更新知识库”
4. 等待系统解析、切分并写入本地向量库

支持文件格式：

- `.txt`
- `.pdf`
- `.docx`
- `.doc`

完成后，系统会在侧边栏显示知识库状态和已登记文件数量。

## 6.2 设计需求转译

用于把自然语言需求自动转为结构化任务书。

操作步骤：

1. 打开“设计任务书”标签页
2. 在输入框中填写景观设计需求
3. 点击“生成数字化任务书”
4. 查看生成结果、规范依据和规则修正说明
5. 点击下载按钮导出 `.json`

建议输入示例：

```text
设计一个适老化社区口袋公园，需要安静、生态、便于轮椅通行，希望设置儿童活动区和林荫休憩空间。
```

系统输出通常包括：

- `project_type`
- `style_preference`
- `canopy_closure`
- `path_slope_max_percentage`
- `hardscape_ratio`
- `functional_zones`
- `warnings`

如果需求中存在明显风险，如坡度过大、儿童区附近配置有毒植物等，系统会自动在 `warnings` 中给出修正说明。

## 6.3 场地现状多维解析

用于生成结构化场地分析报告。

操作步骤：

1. 打开“场地分析”标签页
2. 上传场地资料文件，或直接输入场地描述
3. 点击“生成场地分析报告”
4. 查看结构化分析结果
5. 点击下载按钮导出 `.json`

建议输入示例：

```text
场地位于高密度老旧社区内部，周边以住宅为主，内部有约3米高差，低洼处长期积水，老年人口较多，缺乏休憩与活动空间。
```

系统输出通常包括：

- `location_context`
- `climate_environment`
- `topography_features`
- `opportunities`
- `constraints`
- `design_suggestions`

## 7. 历史结果说明

系统会自动将运行结果保存到项目目录下的 `output_briefs/` 中，带有 `history_` 前缀的 JSON 文件可作为历史工作流记录复用。

页面首页也会展示最近生成的历史记录，便于快速查看。

## 8. 数据文件说明

项目中的关键数据文件如下：

- `data/sense_mapping.json`
  语义映射库，用于把“安静、生态、适老”等感性描述映射为设计倾向
- `data/compliance_rules.json`
  显式规则配置文件，用于控制无障碍坡度、植物风险、湿地提醒等规则
- `chroma_db/`
  本地向量数据库目录
- `output_briefs/`
  输出结果与历史记录目录

## 9. 常见问题

### 9.1 点击生成后提示未填写 API Key

原因：

- 左侧边栏未填写有效 `API Key`

处理方式：

- 填写正确的 `API Key` 后重新执行

### 9.2 提示知识库尚未就绪

原因：

- 还未上传规范文件
- 知识库构建失败

处理方式：

- 先在“知识库管理”中上传规范文件并成功构建

### 9.3 上传文件后解析失败

原因：

- 文件格式异常
- 文档内容过空
- PDF/Word 文本提取质量不足

处理方式：

- 优先上传文本可复制的 PDF / DOCX / TXT 文件
- 必要时将原文整理为 TXT 后重新上传

### 9.4 页面可以打开但没有结果

原因：

- 模型接口未返回有效结果
- 网络连接或模型服务异常

处理方式：

- 检查 `Base URL`、模型名、API Key 是否正确
- 确认所选模型支持文本生成或嵌入能力

## 10. 推荐使用顺序

推荐按以下顺序使用系统：

1. 配置模型参数
2. 上传规范文件并构建知识库
3. 进行设计需求转译
4. 进行场地分析
5. 导出 JSON 结果
6. 查看历史记录并复用结果

## 11. 补充说明

如果你需要继续扩展系统，建议优先修改以下文件：

- `workflow_rules.py`
  扩展显式规则逻辑
- `workflow_llm.py`
  调整提示词与结构化输出
- `workflow_service.py`
  调整整体工作流编排
- `data/compliance_rules.json`
  新增或修改规则阈值

如果只是普通使用，则无需修改代码，按照页面流程操作即可。
